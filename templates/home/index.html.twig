{% extends 'base.html.twig' %}

{% block title %}Accueil - Mini Twitter{% endblock %}

{% block body %}
<div class="w-full min-h-screen bg-[#15202B] p-6 flex flex-col items-center">
    <h1 class="text-4xl font-bold text-white mb-8">Bienvenue sur Mini Twitter</h1>

    <div
        class="w-full max-w-4xl mb-8 text-center flex flex-col md:flex-row items-center justify-center gap-4 animate-fade-in">
        {% if app.user and app.user.avatar %}
        <img src="{{ asset('Uploads/avatars/' ~ app.user.avatar) }}" alt="Avatar de {{ app.user.username }}"
            class="w-10 h-10 rounded-full object-cover border border-neutral-700">
        {% else %}
        <div
            class="w-12 h-12 rounded-full bg-neutral-600 flex items-center justify-center text-white text-base font-bold">
            {{ app.user ? app.user.username|slice(0, 1)|upper : 'M' }}
        </div>
        {% endif %}
        <div>
            <p class="text-neutral-300 text-lg">
                {% if app.user %}
                Bonjour, {{ app.user.username }} ! Partagez vos pensées ou explorez les tweets de la communauté.
                {% else %}
                Découvrez Mini Twitter ! Inscrivez-vous ou connectez-vous pour commencer.
                {% endif %}
            </p>
            <p class="text-neutral-400 text-sm mt-2">Date : {{ "now"|date('d/m/Y H:i', 'Europe/Paris') }}</p>
        </div>
    </div>

    <div class="w-full max-w-4xl flex flex-col md:flex-col items-center justify-between gap-6 animate-fade-in">
     
        <div
            class="bg-[#192734] p-6 rounded-lg shadow-lg border border-neutral-700 w-full hover:bg-[#1c2732] transition duration-200 mb-6">
            <h2 class="text-xl font-semibold text-white mb-4">Dernières actualités</h2>
            <p class="text-neutral-400">
                Restez connecté avec les dernières mises à jour et tweets de vos amis. Mini Twitter est votre plateforme
                pour exprimer vos idées !
            </p>
        </div>

  
        <div class="w-full flex flex-col space-y-4">
            {% if app.user %}
            <a href="{{ path('app_tweet_new') }}"
                class="block bg-[#1DA1F2] p-4 rounded-lg text-white text-center hover:bg-blue-600 transition duration-200 focus:outline-none focus:ring-2 focus:ring-[#1DA1F2]">
                <svg class="w-5 h-5 inline-block mr-2" fill="currentColor" viewBox="0 0 24 24">
                    <path
                        d="M22 5h-6.18C15.4 1.84 11.65 0 7.55 0 3.44 0 0 3.44 0 7.55c0 5.84 5.05 11.94 12.16 11.94 1.38 0 2.96-.23 4.53-.84.97-.41 1.85-1.01 2.6-1.86l2.73 2.73c.44.44 1.15.44 1.59 0 .44-.44.44-1.15 0-1.59l-2.73-2.73c.85-.75 1.45-1.63 1.86-2.6.61-1.57.84-3.15.84-4.53H22c1.1 0 2-.9 2-2s-.9-2-2-2z" />
                </svg>
                Écrire un tweet
            </a>
            {% else %}
            <a href="{{ path('app_login') }}"
                class="block bg-[#1DA1F2] p-4 rounded-lg text-white text-center hover:bg-blue-600 transition duration-200 focus:outline-none focus:ring-2 focus:ring-[#1DA1F2]">
                <svg class="w-5 h-5 inline-block mr-2" fill="currentColor" viewBox="0 0 24 24">
                    <path
                        d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.08 1.99 0 5.97 1.09 6 3.08-1.29 1.94-3.5 3.22-6 3.22z" />
                </svg>
                Se connecter
            </a>
            <a href="{{ path('app_register') }}"
                class="block bg-[#1DA1F2] p-4 rounded-lg text-white text-center hover:bg-blue-600 transition duration-200 focus:outline-none focus:ring-2 focus:ring-[#1DA1F2]">
                <svg class="w-5 h-5 inline-block mr-2" fill="currentColor" viewBox="0 0 24 24">
                    <path
                        d="M15 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm-7 0c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z" />
                </svg>
                S'inscrire
            </a>
            {% endif %}
        </div>
    </div>
</div>

<style>
    .animate-fade-in {
        animation: fadeIn 1s ease-in-out;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
        }

        to {
            opacity: 1;
        }
    }
</style>
{% endblock %}